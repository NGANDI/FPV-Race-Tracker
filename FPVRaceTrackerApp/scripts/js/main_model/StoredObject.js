var main;
(function (main) {
    'use strict';
    var StoredObject = (function () {
        function StoredObject() {
        }
        return StoredObject;
    }());
    main.StoredObject = StoredObject;
})(main || (main = {}));
